/*
var Fizz = require('./fizzbuzz');

describe('fizzBuzz', ()=> {
  it('returns Fizz when given a multiple of 3 that is not also a multiple of 5', () => {
    expect(Fizz.fizzBuzz(3)).toBe('Fizz');
  });
  it('returns Buzz when given a multiple of 5 that is not also a multiple of 3', () => {
    expect(Fizz.fizzBuzz(5)).toBe('Buzz');
  });
  it('returns FizzBuzz when given a multiple of 5 that is also a multiple of 3', () => {
    expect(Fizz.fizzBuzz(15)).toBe('FizzBuzz');
  });
});

describe('createRange', ()=> {
  it('creates a range of given numbers', ()=> {
    expect(Fizz.createRange(1,50)).toContain(2)
  })
  it('creates a range of given numbers', ()=> {
    expect(Fizz.createRange(1,50)).toContain(48)
  })
  it('creates a range of given numbers', ()=> {
    expect(typeof(Fizz.createRange(1,50)) == 'array').toBeTruthy
  })
})
*/